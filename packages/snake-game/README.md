# Snake Game - 主应用

这是贪吃蛇游戏的主应用包，包含了完整的 React 应用。

## 🚀 快速开始

### 开发模式
```bash
pnpm --filter snake-game dev
```

### 构建
```bash
pnpm --filter snake-game build
```

### 预览生产构建
```bash
pnpm --filter snake-game preview
```

## 🎮 游戏控制

- **方向键** 或 **WASD**: 控制蛇的移动
- **空格键**: 暂停/继续/开始游戏
- **鼠标**: 点击界面按钮进行控制

## 🎯 游戏特性

- ✅ 多难度等级（简单、中等、困难、极限）
- ✅ 穿墙模式
- ✅ 道具系统（加速、减速、双倍分数、护盾）
- ✅ 连击系统
- ✅ 本地最高分记录
- ✅ 实时统计面板
- ✅ 视觉反馈效果

## 📁 项目结构

```
snake-game/
├── src/
│   ├── App.tsx          # 根组件
│   ├── main.tsx         # 入口文件
│   └── vite-env.d.ts    # Vite 环境类型
├── index.html           # HTML 模板
├── vite.config.ts       # Vite 配置
└── tsconfig.json        # TypeScript 配置
```

## 🔧 技术栈

- React 18
- TypeScript 5.6
- Vite 5
- @snake/core (游戏逻辑)
- @snake/ui (UI 组件)

## 📦 依赖包

- `@snake/core`: 游戏核心逻辑和类型定义
- `@snake/ui`: 可复用的 UI 组件
- `react`: UI 框架
- `react-dom`: React DOM 渲染

## 🎨 样式设计

游戏采用现代化的暗色主题，包含：
- 渐变背景
- 毛玻璃效果
- 圆角设计
- 响应式布局
- 平滑过渡动画

## 🔍 故障排除

### 端口占用
如果 3000 端口被占用，Vite 会自动尝试下一个端口（3001、3002 等）。

### 模块解析错误
确保先构建依赖包：
```bash
pnpm --filter @snake/core build
pnpm --filter @snake/ui build
```

### 类型错误
运行类型检查：
```bash
pnpm --filter snake-game type-check
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License